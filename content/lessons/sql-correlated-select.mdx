---
title: Correlated in SELECT
chapterSlug: sql-correlated
duration: 5
order: 3
---

## Subquery in SELECT

```sql
SELECT name, marks,
  (SELECT AVG(marks) FROM students
   WHERE course = s.course) AS course_avg
FROM students s;
```

Shows each student's course average.

