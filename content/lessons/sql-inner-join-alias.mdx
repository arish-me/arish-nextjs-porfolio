---
title: Column Aliases in JOINs
chapterSlug: sql-inner-join
duration: 5
order: 4
---

## Avoiding Ambiguity

When both tables have same column name:

```sql
SELECT 
  s.id AS student_id,
  s.name,
  c.id AS course_id,
  c.course_name
FROM students s
INNER JOIN courses c ON s.course_id = c.id;
```

