---
title: Practice - Correlated SELECT
chapterSlug: sql-correlated
duration: 5
order: 4
---

## Exercise: Show Course Max

Show each student with their course's max marks.

## Solution

```sql
SELECT name, marks,
  (SELECT MAX(marks) FROM students
   WHERE course = s.course) AS course_max
FROM students s;
```

